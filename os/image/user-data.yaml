#cloud-config

users:
  # User pi has defaults which we don't need to respecify see /etc/cloud/cloud.cfg
  - name: pi
    plain_text_passwd: copepode
    lock_passwd: false

# rpi:
#   enable_usb_gadget: true

ssh_pwauth: true
